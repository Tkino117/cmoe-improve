# 示したいこと

### 一様配分より探索したSA配分の方が精度が良い

固定推論コスト（N=8・A=6）で、層ごとに x を探索した配分がどの一様配分よりも PPL が低い。

ランダム性について
- 校正のうち、配分を探す / モデルを組む、際の文章は同一のものを用いる
- 校正に用いる文章のみシードを変え5通り実験を行い、有意性を確かめる

実験

- H1: 一様 x=0〜6 を対照として測る
- H2: wikitext2 で校正を行い、wikitext2 と c4-new の両方で測る。構成のbeam search は幅2から4まで試す
- H3: c4 で校正を行い、wikitext2 と c4-new の両方で測る
- H4: wikitext2, c4 による校正をそれぞれベンチマークで測る

### 重心routerより探索したrouterの方が精度が良い

### 上記二つを組み合わせるとさらに精度が向上する
