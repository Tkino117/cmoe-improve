# TODO

設計は [docs/01](docs/01_experiment-platform-design.html)、使い方は [README](README.md)。

## 候補

- **タスク特化の主張を詰める（docs/04）** — 校正の読む位置を採点位置に寄せる線は [report/14](report/14_*scored-positions.md) で打ち切った
- **同じコストなら幅と深さのどちらか（追加測定なし）** — 深さ1（コスト 7.57倍）と、既にある幅4・深さ0（同 2倍）を [report/15](report/15_*search-depth.md) の対照の隣に並べる
- **expert 粒度を上げる（S6A6E16）** — 先に N を振ってオラクル L の床だけ測り、下がるなら本測定へ
- **配分とルーターを同時に動かす** — 足し算にならないことが分かっているので、まず 2×2 で交互作用を見る
- **EW-rule の τ・α を校正で選び直す** — [report/21](report/21_ew-rule-baseline.md) の EW-rule は既定値のままで、負けの大部分が「既定 α が N=8 に合っていない」で説明できてしまう。`allocate.py --grid` が出す格子（τ 9 × α 3）を `cmoe score --oracle suffix_kl` で採点し、校正だけを見て選んだ1本を足す。**査読でここは必ず突かれる**
- **可変 Top-K のしきい値を、その方式自身の軌道で決め直す** — [report/19](report/19_routing-improvements.md) の `dynamic_spectral` は層ごとの出力誤差では最良なのに PPL で負けた。しきい値は「方式1 が伝播させた軌道」の上で決めており、評価では全層がその方式で走る。`Converter` が先頭方式を伝播させる取り決めを、可変 K の方式だけ外して測り直す
- **可変 Top-K の予算を層をまたいで配る** — 今は層ごとに平均 K に合わせている。層をまたいで1本のしきい値にすると、配分（軸5）の探索と同じことをルーター側で連続に行うことになる
